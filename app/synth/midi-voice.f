\ midi voice control
\ basic idea is that on each MIDI event (note on/off and cc), the voice parameters are updated.

\ this should be done in a way that is fully programmable through
\ midi.  it's ok to hard-assign CCs, as it is usually easy enough to
\ change parameters in the midi controller.

\ square

\ pwm
