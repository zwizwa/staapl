/*
void foo(float a, float b) {
    solve(vars(x,y),
          a * x + b * y == q,
          c * x + d * y == r);
    {
        int bar,baz,qux = q, broem = bar;
        int boo = a + b;
        int lala = 123;
        foo(a, b);
        array[x + 10 * y];
    }
}
*/

void bar(void) {
    for (i=0; i<n; i++) {
        array[i] = 0;
    }
}
